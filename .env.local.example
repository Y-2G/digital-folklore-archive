# Firebase Configuration
#
# These environment variables are required for Firebase initialization.
# Copy this file to .env.local and fill in your Firebase project credentials.
#
# IMPORTANT: Never commit .env.local to version control!
# The .env.local file should already be in .gitignore.
#
# To get these values:
# 1. Go to Firebase Console (https://console.firebase.google.com/)
# 2. Select your project
# 3. Go to Project Settings > General
# 4. Scroll down to "Your apps" section
# 5. Select your web app or create a new one
# 6. Copy the configuration values

# ============================================
# Option 1: Production / Remote Firebase
# ============================================
# Firebase API Key (safe to expose on client-side)
NEXT_PUBLIC_FIREBASE_API_KEY=

# Firebase Auth Domain (e.g., your-project.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# Firebase Storage Bucket (e.g., your-project.firebasestorage.app)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=

# Firebase Messaging Sender ID
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=

# Firebase App ID
NEXT_PUBLIC_FIREBASE_APP_ID=

# ============================================
# Option 2: Docker Firebase Emulator (Local Development)
# ============================================
# Uncomment these lines to use Docker-based Firebase Emulator
# When enabled, the above Firebase credentials are not required

# NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true
# NEXT_PUBLIC_FIRESTORE_EMULATOR_HOST=localhost:8080
# NEXT_PUBLIC_AUTH_EMULATOR_HOST=localhost:9099
# NEXT_PUBLIC_STORAGE_EMULATOR_HOST=localhost:9199

# ============================================
# Firebase Admin SDK (Server-side only)
# ============================================
# Required for server actions with elevated privileges
# Choose ONE of the following authentication methods:

# Method 1 (Recommended): Google Application Default Credentials
# Point to your service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Method 2 (Alternative): Explicit Environment Variables
# Extract these values from your service account JSON
# FIREBASE_PROJECT_ID=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_PRIVATE_KEY=

# Note: When using emulators (NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true),
# Admin SDK authentication is not required

# ============================================
# API Authentication (Server-side only)
# ============================================
# Secret API key for item submission API
# Generate a secure random key (e.g., openssl rand -base64 32)
# WARNING: Never expose this in client-side code!
# This variable does NOT have NEXT_PUBLIC_ prefix, keeping it server-side only.
DTA_API_SECRET_KEY=

# Rate Limiting Configuration
# Time window for rate limiting in milliseconds (default: 60000 = 1 minute)
DTA_API_RATE_LIMIT_WINDOW_MS=60000

# Maximum number of requests allowed per window (default: 10 requests per minute)
DTA_API_RATE_LIMIT_MAX_REQUESTS=10
